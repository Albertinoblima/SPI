{
    "name": "@political-research/shared-utils",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "build": "tsc",
        "clean": "rm -rf dist",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:ci": "jest --ci --coverage --maxWorkers=2"
    },
    "dependencies": {
        "server-only": "^0.0.1"
    },
    "devDependencies": {
        "@swc/core": "^1.5.25",
        "@swc/jest": "^0.2.36",
        "@types/jest": "^29.5.12",
        "jest": "^29.7.0",
        "typescript": "^5.4.0"
    }
}